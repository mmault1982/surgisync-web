/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * SurgiScribe API Documentation
 * API documentation with JWT Authentication
 * OpenAPI spec version: v1
 */
import type { Address } from './address';

/**
 * The organization behind a role, with its address book.
 *
 * What is omitted is as decided as what is here:
 *
 * - **`name`.** The role's own `name` is the only name in the payload.
 *   `Company._rename_role_rows` pushes a rename only onto role rows still
 *   carrying the previous name, so a deliberately renamed role row is left
 *   alone rather than reverted — returning both names would publish that
 *   divergence and make each client choose.
 * - **The flat `address_line_1` / `address_line_2` / `city` / `state` /
 *   `zip_code` columns.** `Address` is the source of truth; those are legacy
 *   and are dropped in phase 5's second stage. Publishing them would put a
 *   deprecation into the contract on day one.
 * - **`parent_company`.** It is the tenant, and the role's `is_owned` already
 *   answers the only question a client has about it.
 * - **`is_active` / `deleted_at`.** A soft-deleted Company under a live role
 *   is an integrity problem, not a client concern.
 */
export interface Company {
  readonly id: number;
  /** @maxLength 20 */
  phone?: string;
  /** @maxLength 20 */
  fax?: string;
  /** @maxLength 254 */
  email?: string;
  /** @maxLength 100 */
  contact_name?: string;
  /** @maxLength 100 */
  contact_title?: string;
  /** @maxLength 254 */
  contact_email?: string;
  /** @maxLength 20 */
  contact_phone?: string;
  /** @maxLength 100 */
  billing_contact_name?: string;
  /** @maxLength 254 */
  billing_contact_email?: string;
  /** @maxLength 20 */
  billing_contact_phone?: string;
  /** Read-only here. Add, amend and remove addresses through /api/v1/directory/companies/{id}/addresses/, which puts the owning organization in the URL — it may be shared with this organization's other roles. */
  readonly addresses: readonly Address[];
}
