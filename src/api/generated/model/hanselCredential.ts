/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * SurgiScribe API Documentation
 * API documentation with JWT Authentication
 * OpenAPI spec version: v1
 */

/**
 * Read and write. One serializer, not a List/Detail pair — the two would be
 * byte-identical here (no heavy relations, no derived counts) and would put two
 * components in the schema for one shape.
 */
export interface HanselCredential {
  readonly id: number;
  /** The organization these credentials belong to. */
  parent_company: number;
  /** @nullable */
  readonly parent_company_name: string | null;
  /**
     * Hansel OAuth client id. Not secret.
     * @maxLength 255
     */
  client_id: string;
  /** Hansel workspace UUID. Sent only as the OAuth scope `workspace:<uuid>`. */
  workspace_id: string;
  /** Whether a secret is stored. The secret itself is never returned. */
  readonly client_secret_set: boolean;
  /** Last 4 characters of the secret, so the UI can show which one is stored. */
  readonly client_secret_last4: string;
  /** False when the stored secret was encrypted in a different environment (e.g. a production snapshot restored into staging). The credential looks configured but cannot be used — re-enter it. */
  readonly secret_readable: boolean;
  /** Operational state of the integration (distinct from soft-delete). */
  is_active?: boolean;
  /**
     * When these credentials last exchanged successfully for a token.
     * @nullable
     */
  readonly last_verified_at: string | null;
  /** Failure code from the last verification attempt; empty when the last one passed. */
  readonly last_verification_error: string;
  readonly created_at: string;
  readonly updated_at: string;
}
