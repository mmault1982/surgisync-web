/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * SurgiScribe API Documentation
 * API documentation with JWT Authentication
 * OpenAPI spec version: v1
 */
import type { Company } from './company';

/**
 * One manufacturer and everything attached to it.
 *
 * The composite read: one request returns the role, the identity behind it,
 * and that identity's addresses. Three round trips would reassemble a join on
 * mobile, where latency is the cost that matters, and would make both clients
 * learn the role -> company -> addresses chain — which is exactly the
 * internal structure still in motion.
 *
 * `company.id` is not decoration. It is what lets a client notice that two
 * roles share one address book *before* someone edits it.
 *
 * The list keeps the lean `ManufacturerSerializer`: a catalog page is 500 rows
 * and none of them needs an address book.
 */
export interface ManufacturerDetail {
  readonly id: number;
  /** @maxLength 100 */
  name: string;
  /** @nullable */
  barcode?: string | null;
  /** True when this row belongs to your organization and may be renamed or removed. */
  readonly is_owned: boolean;
  readonly company: Company;
}
