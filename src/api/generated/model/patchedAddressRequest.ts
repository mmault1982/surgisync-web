/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * SurgiScribe API Documentation
 * API documentation with JWT Authentication
 * OpenAPI spec version: v1
 */
import type { AddressKindEnum } from './addressKindEnum';

/**
 * One address in an organization's address book. Read and write.
 *
 * `company` is deliberately not a field: it comes from the URL, which is what
 * makes "forgot to scope this to the company" unrepresentable rather than
 * merely untested — the same rule `PartComponentWriteSerializer` states for
 * the kit it belongs to.
 *
 * `is_primary` is writable, and setting it is a *transition* rather than a
 * field assignment: `unique_primary_address_per_company_kind` is a partial
 * unique index and `Address` has no `save()` hook, so a second primary of a
 * kind would raise `IntegrityError` rather than replace the incumbent. The
 * view demotes the sibling in the same transaction; see
 * `inventory/views/companies.py`.
 */
export interface PatchedAddressRequest {
  /**
     * What this address is used for.
     *
     * * `physical` - Physical
     * * `shipping` - Shipping / Receiving
     * * `billing` - Billing
     * * `mailing` - Mailing
     */
  kind?: AddressKindEnum;
  /** The default address of this kind. At most one per organization per kind. */
  is_primary?: boolean;
  /**
     * Optional name for this location (e.g. 'Main Campus', 'Loading Dock B').
     * @maxLength 100
     */
  label?: string;
  /** @maxLength 200 */
  address_line_1?: string;
  /** @maxLength 200 */
  address_line_2?: string;
  /** @maxLength 100 */
  city?: string;
  /** @maxLength 50 */
  state?: string;
  /** @maxLength 20 */
  zip_code?: string;
  /**
     * ISO 3166-1 alpha-2 country code.
     * @minLength 1
     * @maxLength 2
     */
  country?: string;
  /**
     * Contact person at this address (e.g. the receiving dock).
     * @maxLength 100
     */
  contact_name?: string;
  /** @maxLength 20 */
  phone?: string;
  /** Special delivery instructions (loading dock hours, check-in procedures, etc.). */
  instructions?: string;
}
