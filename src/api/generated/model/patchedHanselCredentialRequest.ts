/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * SurgiScribe API Documentation
 * API documentation with JWT Authentication
 * OpenAPI spec version: v1
 */

/**
 * Read and write. One serializer, not a List/Detail pair — the two would be
 * byte-identical here (no heavy relations, no derived counts) and would put two
 * components in the schema for one shape.
 */
export interface PatchedHanselCredentialRequest {
  /** The organization these credentials belong to. */
  parent_company?: number;
  /**
     * Hansel OAuth client id. Not secret.
     * @minLength 1
     * @maxLength 255
     */
  client_id?: string;
  /** Hansel workspace UUID. Sent only as the OAuth scope `workspace:<uuid>`. */
  workspace_id?: string;
  /**
     * Hansel OAuth client secret. Write-only — never returned. Omit on PATCH to keep the stored value.
     * @minLength 8
     * @maxLength 512
     */
  client_secret?: string;
  /** Operational state of the integration (distinct from soft-delete). */
  is_active?: boolean;
  /** Use this workspace for outbound asset sync. At most one active credential per organization may have this set. */
  sync_enabled?: boolean;
  /**
     * Hansel manufacturer UUID used when a part's manufacturer has no explicit mapping. Required to create assets, since Hansel's asset create demands a manufacturer from their UUID space.
     * @nullable
     */
  default_manufacturer_id?: string | null;
  /**
     * Hansel asset-type UUID applied to every asset we create. Required to create assets; we have no local equivalent to map from.
     * @nullable
     */
  default_asset_type_id?: string | null;
}
